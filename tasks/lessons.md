# Lessons

Format: `YYYY-MM-DD | trigger | wrong move | right move | why`

2026-05-15 | resuming BMC/NCSI research on live iDRAC6 | re-probed 192.168.0.23 NCSI from scratch, rediscovered March-2026 finding, burned a probe round-trip | read the author's BMC research corpus (esp. the dell-ncsi-shared-nic investigation) BEFORE touching live hardware | prior work already pinned the blocker; live re-probe added nothing but latency
2026-05-15 | quoting a path from a doc | said "/tmp/dell-bmc-kernel.bin" as if local; it was on the BMC (tmpfs, wiped on reboot) | state where a path is rooted; copy volatile BMC artifacts to durable storage at extraction time | lost the original kernel dump to a BMC reboot; had to re-extract
2026-05-15 | extracting kernel from /dev/mem | used count= from doc's "~4.2MB" prose, truncated last 325KB (incl. kernel-data) | compute exact bounds from /proc/iomem (text+data start/end), bs=512 sector-aligned | every byte of a forensic dump is load-bearing; prose sizes are approximate
2026-05-15 | user wants to install + run scripts | repeatedly pushed pipx/venv as "the way"; pipx isolates the lib so `import zipmi` fails in user scripts | clone→one command (./install.sh): global pip, auto-fallback to --user on PEP668; lib importable + CLI on PATH | user means the normal python flow, not app-sandbox; pipx is CLI-only
2026-05-15 | hardcoded calvin/root in tests "leak", "change tests" | almost re-vectored crypto oracle tests | inspect first: test_md5/rakp are deterministic offline vectors (expected digests are functions of "calvin", the public Dell default), conftest.py gates @pytest.mark.live behind ZIPMI_LIVE_TARGET, integration uses in-process vbmc — `env -u ZIPMI_* pytest` = 66 green, zero hardware | suite already hermetic by design; "calvin" is intentional vector input, not a credential — changing breaks math for no gain
2026-05-15 | linking a repo asset for click-through (wire-trace.svg) | used hardcoded raw.githubusercontent.com URL; zenfish/zipmi is PRIVATE so that host 404s (no auth) | relative `path?raw=true` link → GitHub's authenticated raw redirect; works private + public, no hardcoded owner/branch | raw.githubusercontent has no auth; private repos only reachable via the github.com authed redirect
2026-05-24 | `make install` ships old code; user re-hit it 3x | patched one layer at a time (--force-reinstall, then --no-cache-dir) — symptom whack-a-mole | when an install is stale, suspect ALL layers at once: (1) version-pin no-op, (2) pip wheel cache, (3) stale build/ dir setuptools reuses by mtime; rm -rf build dist *.egg-info before build/install; add a verify guard that imports from a NON-repo cwd and byte-compares installed CLI vs checkout (fails loudly) | the ./zipmi shadow on sys.path[0] made `verify` always print the checkout path, masking stale site-packages installs; build/ mtime-skip was the real repeat offender behind two "fixed" attempts
2026-06-12 | source edits "work in python -c but CLI still fails" (cipher-17 debug) | spent ~15 tool calls re-deriving crypto that was already correct, because `cd repo && python3 -c` picked up edits via cwd-shadow while `/opt/homebrew/bin/zipmi` ran STALE site-packages | FIRST thing when CLI≠source: `python3 -c "import zipmi.core; print(zipmi.core.__file__)"` from a NON-repo cwd; if it's site-packages not the checkout, `pip install -e .` before debugging anything. This is the 2026-05-24 lesson recurring — heed it | the non-editable install masked every fix (const, retry, OEM modules); the bug was real but invisible to the CLI until editable-installed
2026-06-12 | OpenBMC cipher-17 (SHA256) RMCP+ in-session msgs silently dropped, RAKP fine | assumed K1/K2 derivation `b"\x01"*len(sik)` was right (it works for cipher 3) | IPMI 2.0 §13.32 key-deriv constants are FIXED 20 bytes (Const1/2 = 0x01/0x02 ×20) regardless of hash — verified vs phosphor-net-ipmid rmcp.hpp + ipmitool -vvv known-answer K1/K2. SHA256 SIK is 32B so len(sik) gave a 32B const → wrong keys; cipher3 (SIK=20) only coincidentally worked | the auth-only RAKP codes don't use K1/K2, so a K1/K2 bug passes the whole handshake and only the first encrypted message reveals it (BMC drops on integrity fail, no error reply)
2026-06-12 | OpenBMC dropped first encrypted msg AFTER keys were proven correct (replay of same bytes worked) | chased a phantom framing bug for many calls | it was a netipmid RACE: the first encrypted packet can reach the BMC before applyIntegrityAlgo finishes installing the session K1 → "Packet Integrity check failed" once, then fine. Fix = UDP retransmit-on-timeout (what every IPMI client does; ipmitool defaults to 4). zipmi had NO retry | IPMI rides UDP; "no retry" is a latent bug that a lossy link or a BMC setup-race exposes. Always retry on timeout
2026-08-02 | vbmc idrac10 start sticks | assumed cold-boot hang | fast path ran restore-idrac10.sh synchronously in $(vbmc_boot); also cold-path background subshells leaked the cmdsub pipe (redirect on inner cmd, not subshell) | $(...) waits for EOF on all fds; any & child inheriting stdout holds it open — detach + wrap redirect around the whole ( ) &
2026-08-02 | idrac10 warm-restore 'no serial console / npcm845 UART vmstate gap' | trusted the box comment, almost patched+rebuilt qemu | proved console live via sudo python probe -> real bug was vbmc_ssh reading wrong socket path (/tmp/... vs ckpt/rserial.sock) + root-owned needs sudo | verify the claim empirically before accepting a 'deep' root cause; 16550 SerialMM has full vmstate_serial_mm — no gap existed
2026-08-02 | claimed "zipmi only carries idrac9/10 OEM" | stated a codebase limit from stale session-context inference | grepped scapy_ipmi/oem/ — 18 vendors present | never assert a codebase "only/limit" claim without grepping first; recent-context blobs bias toward what was recently discussed
2026-08-02 | release sweep: told subagents to delete dead-path lines + collapse dupes | user then said "don't lose reader context" | killed agents, reverted, re-ran with "preserve every line, swap only the path token" | genericize != delete; on release cleanup default to preserving all descriptive context, only strip the private path token
2026-08-02 | autouse conftest fixture reset OEM registry every test | broke load_vendor tests (7 fail) — register() runs once at import, restore-to-empty leaves later load_vendor calls no-op due to import memoization | scoped the clean-registry fixture to only the firewall tests that need it | global mutable + import memoization: per-test reset conflicts with modules that register at import time
2026-08-02 | standalone-repo release | private paths in code: 1 KB dep (bmc_id) + 6 parser src= defaults + ~95 docstring refs | bundled KB+sources into zipmi/data/ (package-data), repointed defaults to Path(__file__)-relative, genericized docstrings | "no external deps" = pull functional data into the package + make defaults package-relative, not just scrub comments
2026-08-10 | zmd2html moved user README.md into .md/ stash by default | format/conversion utility silently relocated the source file | conversation utilities must be non-destructive: read input, emit to stdout, write only when -o/dest given; never move/delete source | surprising data-move in a 'converter' looks like data loss
2026-08-12 | zipmi edits not taking effect via `zipmi` binary | assumed source ran; chased phantom stale-code bug | site-packages had a COPY (regular pip install), shadowing source unless cwd=source; ran `pip install -e .` | now editable, edits land immediately
2026-08-13 | subagent scoped NetFn 0x04, said "M/B/exponent linearization helper missing, must add" | it grepped commands.py only | the helper existed in sdr_full.py (parse_full_sdr/cook_reading) and cmd_sensor_get already used it | before declaring a helper missing, grep the WHOLE package, not the one obvious module
2026-08-13 | subagent decoded Get Channel Payload Version BCD as major=low nibble | live read showed SOL "0.1" (should be 1.0) | spec §24.8: MS nibble=major, LS=minor; flipped it | verify BCD/bitfield nibble order against a KNOWN value (SOL payload = 1.0), not just "a nibble"
2026-08-13 | `vbmc megarac-hpe ipmi chassis poh` printed a format my code never emits ("0 days, 20 hours") | assumed a stale/divergent zipmi checkout | vbmc lets a box define vbmc_ipmi() that shells to ipmitool (megarac/X14 do); default path runs my source (same-inode symlink) | vbmc per-box overrides may be ipmitool, not zipmi — check for vbmc_ipmi() before trusting `vbmc ipmi` output as zipmi's
2026-08-14 | zipmi/oem-lineage | framed user's illustrative example (AMI-as-Dell-ODM) as a formal "hypothesis" to falsify | treat examples as examples; do the general check across ALL items | user gives an example to illustrate scope, not a claim to test
